name: detect-unused-secrets

on: [push]

permissions: write-all

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Detect unused secrets
        uses: nearform/github-action-detect-unused-secrets@fix/es-module-scope
        with:
          github-token: ${{ secrets.actions_token  }}
